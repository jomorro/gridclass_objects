const gridConfig = {
    rowCount: 10,
    columnCount: 10,
    cellHeight: "20px",
    cellWidth: "20px",
    parentElement: document.getElementById("gridContainer"),
    gridId: "grid",
}

const grid = new Grid(gridConfig) 